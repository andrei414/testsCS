pipeline {
    agent any
    stages {
        stage('check shellcheck') {
            steps {
                bash '''#!/bin/bash
                 echo "hello world" 
                '''
            }
        }
    }
}